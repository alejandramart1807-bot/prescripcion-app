{
  "name": "@tinterno/engine",
  "private": true,
  "version": "0.0.0",
  "type": "module",
  "description": "Motor de cálculo de dosis de Tinterno (TypeScript puro, sin DOM). Extraído de index.html.",
  "main": "src/index.ts",
  "types": "src/index.ts",
  "scripts": {
    "test": "vitest run",
    "test:watch": "vitest",
    "typecheck": "tsc --noEmit"
  },
  "devDependencies": {
    "typescript": "^5.7.2",
    "vitest": "^3.2.4"
  }
}
